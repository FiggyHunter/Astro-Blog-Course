---
// interface Props {
//   postTitle: string;
//   authorName: string;
//   postURL: string;
//   postDATE: string;
// }

import { slugify, formatDate } from "@/js/utils";

const { data, url } = Astro.props;
const { title, date, author } = data;
const base = import.meta.env.BASE_URL.slice(0, -1);
---

<div class="post">
  <h3 class="h4"><a href=`${url}/`>{title}</a></h3>
  <small
    ><a href=`${base}/author/${slugify(author)}`>{author}</a> • {
      formatDate(date)
    }</small
  >
</div>

<style>
  .h3 {
    justify-self: center;
  }
</style>
